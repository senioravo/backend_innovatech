PORT=3001
NODE_ENV=development

# GlitchTip — app.glitchtip.com → Settings → Client Keys (DSN)
# SENTRY_DSN=https://<public-key>@app.glitchtip.com/<project-id>
# SENTRY_ENVIRONMENT=development

USERS_SERVICE_URL=http://localhost:3003
INTERNAL_SERVICE_TOKEN=development-token
