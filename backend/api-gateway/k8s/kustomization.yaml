apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: api-gateway-config
    files:
      - krakend.json

resources:
  - deployment.yaml
  - service.yaml
