apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - users-db.yaml
  - pm-db.yaml
  - flyway-users-job.yaml
  - flyway-pm-job.yaml

# Flyway SQL ConfigMaps are generated in backend/kustomization.yaml (paths to
# ms-*/db/flyway cannot be referenced from this subdirectory due to kustomize
# load restrictions).
