apiVersion: v1
kind: Service
metadata:
  name: ms-kpi
  labels:
    app.kubernetes.io/name: ms-kpi
    app.kubernetes.io/part-of: innovatech
    app.kubernetes.io/component: microservice
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ms-kpi
  ports:
    - name: http
      port: 3004
      targetPort: http
      protocol: TCP
