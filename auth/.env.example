# AS-TASK-04: PostgreSQL Database Configuration
PORT=3001

# AS-TASK-06: JWT Configuration (Validación y generación segura)
JWT_SECRET=your_secret_key_here_change_in_production
JWT_EXPIRES_IN=1h
JWT_ISSUER=innovatech-auth

# AS-TASK-19: PostgreSQL Connection - Opción 1 (Neon, Heroku, Railway)
# DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# AS-TASK-19: PostgreSQL Connection - Opción 2 (Local o variables separadas)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=innovatech_db
DB_USER=postgres
DB_PASSWORD=your_postgres_password

# AS-TASK-17: Bcrypt Configuration
BCRYPT_SALT_ROUNDS=10

# AS-TASK-03: Circuit Breaker Configuration
CIRCUIT_BREAKER_TIMEOUT=3000
CIRCUIT_BREAKER_ERROR_THRESHOLD=50
CIRCUIT_BREAKER_RESET_TIMEOUT=30000

# AS-TASK-23: Elasticsearch - Auditoría centralizada (opcional)
# Sin ELASTICSEARCH_NODE solo se usan logs locales con Winston
# Ejemplo: https://mi-cluster.es.region.cloud.es.io:443
# ELASTICSEARCH_NODE=
# ELASTICSEARCH_AUDIT_INDEX=auth-audit
# Autenticación: API key (recomendado en Elastic Cloud) o usuario/contraseña
# ELASTICSEARCH_API_KEY=base64ApiKeyHere==
# ELASTICSEARCH_USERNAME=elastic
# ELASTICSEARCH_PASSWORD=
# Solo desarrollo / cert autofirmado: ELASTICSEARCH_TLS_INSECURE=1
